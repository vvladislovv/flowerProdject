# Документация по функционалу приложения

## 📋 Обзор

Это мини-приложение для цветочного магазина, реализующее полный цикл покупки: от просмотра каталога до отслеживания доставки.

## 🔄 Пользовательские сценарии

### 1. Первый запуск и регистрация

**Путь:** `/welcome` → `/signin` или `/signup` → `/home`

1. Пользователь видит экран приветствия с 3 шагами онбординга
2. Переход к экрану входа/регистрации
3. После авторизации перенаправление на главную страницу
4. Данные пользователя сохраняются в `localStorage` под ключом `flowers_user`

### 2. Просмотр каталога и поиск

**Путь:** `/products` → `/products/[id]`

1. Пользователь видит список всех товаров
2. Доступны фильтры:
   - Категория (Bouquets, Indoor, Flower)
   - Ценовой диапазон (Min/Max)
   - Рейтинг (1-5 звезд)
3. Поиск по названию, описанию, категории
4. Переключение между сеткой и списком
5. При клике на товар открывается детальная страница

### 3. Детали товара

**Путь:** `/products/[id]`

**Функционал:**
- Просмотр изображений (основное + миниатюры)
- Информация о товаре (название, цена, описание, размер)
- Рейтинг и количество отзывов
- Выбор количества
- Добавление в корзину
- Добавление в избранное
- Переход к отзывам

### 4. Создание кастомного букета

**Путь:** `/custom-bouquet`

**Процесс:**
1. Выбор размера (Small - 5 цветов, Medium - 10, Large - 15)
2. Выбор типов цветов (Roses, Lilies, Tulips, Peonies, Hyacinths)
3. Указание количества каждого типа цветов
4. Добавление персонального сообщения (опционально)
5. Автоматический расчет стоимости
6. Добавление в корзину как кастомный товар

**Расчет цены:**
- Базовая цена размера + (цена цветка × количество)

### 5. Корзина покупок

**Путь:** `/cart`

**Функционал:**
- Просмотр всех товаров в корзине
- Изменение количества товаров
- Удаление товаров
- Применение промокодов:
  - `FLOWER20` - скидка 20%
  - `WELCOME10` - скидка 10%
- Расчет стоимости:
  - Subtotal (сумма товаров)
  - Delivery Fee ($15)
  - Discount (если применен промокод)
  - Total (итоговая сумма)

### 6. Оформление заказа

**Путь:** `/checkout` → `/checkout/payment`

**Шаг 1: Billing (`/checkout`)**
- Личные данные (имя, email, телефон)
- Адрес доставки (страна, штат, адрес, почтовый индекс)
- Выбор даты доставки (минимум завтра)
- Выбор времени доставки (9:00 - 18:00)
- Текст для открытки (опционально)
- Данные сохраняются в `sessionStorage`

**Шаг 2: Payment (`/checkout/payment`)**
- Выбор метода оплаты (Master Card)
- Данные карты:
  - Имя на карте
  - Номер карты
  - Срок действия (MM/YY)
  - CVV код
- Опция сохранения карты
- Подтверждение заказа

**После оплаты:**
- Создание заказа с уникальным ID
- Сохранение в `localStorage` под ключом `flowers_orders`
- Очистка корзины
- Перенаправление на страницу отслеживания

### 7. Отслеживание заказа

**Путь:** `/orders/[id]`

**Статусы заказа:**
1. **Confirmed** (✓) - Заказ подтвержден
2. **Preparing** (🌺) - Букет готовится
3. **On the way** (🚗) - В пути
4. **Delivered** (🎉) - Доставлен

**Информация:**
- Номер заказа
- Список товаров с изображениями
- Итоговая стоимость
- Дата и время доставки
- Адрес доставки
- Кнопка связи с флористом

### 8. Чат с флористом

**Путь:** `/chat?order=[id]`

**Функционал:**
- Обмен сообщениями в реальном времени
- Отображение времени сообщений
- Автоматические ответы флориста (симуляция)
- История переписки
- Сообщения сохраняются в состоянии компонента

### 9. Отзывы

**Путь:** `/products/[id]/reviews`

**Функционал:**
- Просмотр всех отзывов на товар
- Средний рейтинг
- Распределение по звездам
- Написание нового отзыва:
  - Выбор рейтинга (1-5 звезд)
  - Текст отзыва
  - Сохранение в `localStorage`

### 10. Избранное

**Путь:** `/wishlist`

**Функционал:**
- Вкладка "Products" - избранные товары
- Вкладка "Florists" - избранные флористы
- Добавление/удаление из избранного
- Данные хранятся в `localStorage` под ключом `flowers_wishlist`

### 11. Поиск

**Путь:** `/search`

**Функционал:**
- Поиск по названию, описанию, категории
- История поиска (последние 5 запросов)
- Популярные запросы
- Результаты в реальном времени

## 💾 Структура данных

### User (Пользователь)
```typescript
{
  id: string
  name: string
  email: string
  phone?: string
  location?: string
}
```

### Product (Товар)
```typescript
{
  id: string
  name: string
  price: number
  image: string
  category: string
  occasion?: string[]
  flowers?: string[]
  rating: number
  reviewsCount: number
  description: string
  seller?: string
  size?: string
}
```

### CartItem (Товар в корзине)
```typescript
{
  product: Product
  quantity: number
  customMessage?: string
}
```

### Order (Заказ)
```typescript
{
  id: string
  userId: string
  items: CartItem[]
  subtotal: number
  deliveryFee: number
  total: number
  deliveryDate: string
  deliveryTime: string
  billing: BillingInfo
  payment: PaymentInfo
  status: 'pending' | 'confirmed' | 'preparing' | 'delivering' | 'delivered' | 'cancelled'
  createdAt: string
  cardMessage?: string
}
```

## 🔧 API localStorage

Все функции работы с данными находятся в `lib/storage.ts`:

### Пользователь
- `getUser()` - получить текущего пользователя
- `setUser(user)` - установить пользователя
- `setUser(null)` - выход

### Корзина
- `getCart()` - получить корзину
- `setCart(cart)` - установить корзину
- `addToCart(item)` - добавить товар
- `removeFromCart(productId)` - удалить товар
- `updateCartItemQuantity(productId, quantity)` - изменить количество
- `clearCart()` - очистить корзину

### Заказы
- `getOrders()` - получить все заказы
- `addOrder(order)` - добавить заказ

### Избранное
- `getWishlist()` - получить список ID избранных товаров
- `toggleWishlist(productId)` - добавить/удалить из избранного

### Товары
- `getProducts()` - получить все товары (с инициализацией мок-данных)

### Отзывы
- `getReviews(productId?)` - получить отзывы (все или по товару)
- `addReview(review)` - добавить отзыв

## 🎨 Компоненты

### Header
Универсальная шапка с настройками:
- `title` - заголовок
- `showBack` - кнопка "Назад"
- `showCart` - иконка корзины с счетчиком
- `showSearch` - иконка поиска
- `showMenu` - иконка меню

### BottomNav
Нижняя навигация с 5 пунктами:
- Главная
- Каталог
- Избранное
- Чат
- Профиль

### Button
Кнопка с вариантами:
- `variant`: 'primary' | 'secondary' | 'outline'
- `color`: 'green' | 'purple'
- `fullWidth` - на всю ширину

### Input
Поле ввода с:
- `label` - подпись
- `error` - сообщение об ошибке
- Стандартные HTML атрибуты

## 🚀 Развертывание

### Локальная разработка
```bash
npm install
npm run dev
```

### Продакшен сборка
```bash
npm run build
npm start
```

### Vercel
Проект готов к развертыванию на Vercel:
1. Подключить GitHub репозиторий
2. Vercel автоматически определит Next.js
3. Деплой произойдет автоматически

## 📱 Мобильная оптимизация

- Все страницы адаптированы под мобильные устройства
- Максимальная ширина контента: 448px (max-w-md)
- Touch-friendly элементы (минимум 44x44px)
- Плавные анимации и переходы
- Оптимизированные изображения через Next.js Image

## 🔐 Безопасность

**Важно:** Это демо-приложение использует localStorage и не имеет реальной авторизации или платежей.

Для продакшена необходимо:
- Реальная авторизация (JWT, OAuth)
- Защита API endpoints
- Валидация данных на сервере
- Шифрование платежных данных
- HTTPS обязателен

## 🐛 Отладка

### Очистка данных
Для сброса всех данных в браузере:
```javascript
localStorage.clear()
sessionStorage.clear()
```

### Просмотр данных
В консоли браузера:
```javascript
// Пользователь
JSON.parse(localStorage.getItem('flowers_user'))

// Корзина
JSON.parse(localStorage.getItem('flowers_cart'))

// Заказы
JSON.parse(localStorage.getItem('flowers_orders'))
```

## 📝 Примечания

1. Все изображения загружаются с Unsplash (можно заменить)
2. Чат работает локально без WebSocket
3. Промокоды жестко закодированы (FLOWER20, WELCOME10)
4. Статусы заказов не обновляются автоматически
5. Нет реальных уведомлений (push notifications)

## 🔄 Интеграция с бэкендом

Для интеграции с реальным API:

1. Заменить функции в `lib/storage.ts` на API вызовы
2. Добавить обработку ошибок
3. Добавить loading состояния
4. Настроить аутентификацию
5. Добавить refresh tokens
6. Реализовать WebSocket для чата

Пример:
```typescript
// Вместо localStorage
const response = await fetch('/api/cart', {
  headers: { Authorization: `Bearer ${token}` }
})
const cart = await response.json()
```

