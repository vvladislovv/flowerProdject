# Быстрый старт

## 🚀 Запуск проекта

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 3. Сборка для продакшена

```bash
npm run build
npm start
```

## 📱 Тестовые данные

При первом запуске автоматически создаются мок-данные:
- 6 товаров в каталоге
- Все данные хранятся в localStorage браузера

## 🔑 Тестовые промокоды

- `FLOWER20` - скидка 20%
- `WELCOME10` - скидка 10%

## 🧪 Тестовый пользователь

Для входа используйте любые данные:
- Email: `test@example.com`
- Password: любой (минимум 6 символов)

Или используйте социальные сети (mock авторизация).

## 📂 Основные маршруты

- `/welcome` - Онбординг
- `/signin` - Вход
- `/home` - Главная
- `/products` - Каталог
- `/cart` - Корзина
- `/custom-bouquet` - Кастомный букет
- `/checkout` - Оформление заказа
- `/orders` - Мои заказы
- `/chat` - Чат с флористом
- `/wishlist` - Избранное

## 🎨 Цветовая схема

- Зеленый: `#10B981` (primary-green)
- Фиолетовый: `#8B5CF6` (primary-purple)

## 🔧 Настройка для Vercel

Проект готов к деплою на Vercel:

1. Загрузите код в GitHub
2. Подключите репозиторий к Vercel
3. Vercel автоматически определит Next.js и настроит сборку
4. Деплой произойдет автоматически

Или используйте Vercel CLI:

```bash
npm i -g vercel
vercel login
vercel
```

## 💡 Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Запуск продакшена
npm start

# Линтинг
npm run lint
```

## 🐛 Отладка

### Очистка данных браузера

В консоли браузера:
```javascript
localStorage.clear()
sessionStorage.clear()
location.reload()
```

### Просмотр данных

```javascript
// Пользователь
console.log(JSON.parse(localStorage.getItem('flowers_user')))

// Корзина
console.log(JSON.parse(localStorage.getItem('flowers_cart')))

// Заказы
console.log(JSON.parse(localStorage.getItem('flowers_orders')))
```

## 📚 Дополнительная документация

- `README.md` - Полная документация проекта
- `DOCUMENTATION.md` - Детальное описание функционала

